(function () {
    let message: string = 'Although';
    let number: number = 123;
    let boolean: boolean = true;
    let today: Date = new Date();

    // let any: string | number | Date | boolean;
    let any;
    any = message;
    any = number;
    any = boolean;
    any = today;

    let grimm = {
        name: 'Nick',
        age:30
    };

    grimm = {
        name: 'troble',
        age: 28,
        // state: 'activo'
    }
})();
